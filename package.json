{
  "name": "cypress-pact",
  "version": "1.0.0",
  "description": "An experiment to see if pacts can be generated automagically with cypress tests",
  "main": "index.js",
  "scripts": {
    "test": "cypress:open",
    "cypress": "cypress run",
    "cypress:headed": "cypress run --headed",
    "cypress:open": "cypress open --detached"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^3.1.5"
  }
}
